<!DOCTYPE html>
<html lang="en">
<% include header %>
<body>
  <% include navbar %>
  
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-anchor"></span> Your Public Profile</h1>
    </div>

    <div class="alert alert-info" role="alert">The information you fill here will be publicly visible.</div>

      <% if (userRole == null) { %>
      <% } %>

      <form>
	<div class="form-group">
	  <label for="userRealName">Your name:</label>
	  <input type="text" class="form-control" id="userRealName" placeholder="Your full name">
	</div>

	<div class="form-group">
	  <label for="userDescription">Tell about you to others:</label>
	  <textarea id="userDescription" class="form-control" rows="3" placeholder="Skills, experience, interests, ..."></textarea>
	</div>

	<div class="form-group">
          <label for="userDescription">Your skills:</label>
	  
	  List skills from db and allow user to choose: undefined, no skills, average, expert 
	  
	</div>

	<p>Sites where people can find more information of you (for example, personal website, LinkedIn profile page, ...):</p>

	<ul id="more_info_site_list">
	<li>None yet
	</ul>

	<p><button id="user_more_info_site_button" type="button" class="btn btn-info">Add site</button></p>

	<p>
	<button id="profile_save_button" type="button" class="btn btn-primary disabled">Save changes</button></p>
      </form>

    <hr>

    <p>List of ideas you have added:</p>

    <ul id="idea_list">
    <li>None yet
    </ul>

    <hr>
    <% include footer %>
  </div>
  <!-- /container -->
  <script src="https://code.jquery.com/jquery.js"></script>
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>
  <script type="text/javascript">
    function htmlDecode(input){
    var e = document.createElement('div');
    e.innerHTML = input;
    return e.childNodes.length === 0 ? "" : e.childNodes[0].nodeValue;
    }
    var user = JSON.parse(htmlDecode("<%= JSON.stringify(user) %>"));
    var userRole = JSON.parse(htmlDecode("<%= JSON.stringify(userRole) %>"));
    var roles = JSON.parse(htmlDecode("<%= JSON.stringify(roles) %>"));
  </script>
  <script type="text/javascript" src="/js/profile.js"></script>
</body>
</html>
